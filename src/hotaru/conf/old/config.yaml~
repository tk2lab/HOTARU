defaults:
  - _self_
  - model
  - override hydra/output: hotaru
  - override hydra/job_logging: hotaru

num_devices: null
device_memsize: null

batch_frame: 100
block_size: 100

interface: CUI
max_train_step: 10
force: false

output_dir: outputs
labels: ${hydra:runtime.choices}

hydra:
  run:
    dir: ${output_dir}/log
  job:
    name: hotaru
    env_set:
      JAX_PLATFORMS: cuda
      JAX_LOG_COMPILES: false
