dir: outputs
stats:
  dir: ${data.label}
  files:
    - data_stats.npz
    - data_max.npy
    - data_std.npy
    - data_cor.npy
find:
  dir: ${data.label}${radius.label}
  files:
    - find.npz
make:
  dir: ${data.label}${radius.label}${select.label}
  files:
    - "{stage:03d}footprint.npy"
    - "{stage:03d}stats.csv"
spatial:
  dir: ${data.label}${radius.label}${select.label}${model.label}
  files:
    - "{stage:03d}footprints.npy"
    - "{stage:03d}stats.csv"
temporal:
  dir: ${data.label}${radius.label}${select.label}${model.label}
  files:
    - "{stage:03d}spike.npy"
    - "{stage:03d}background.npy"
eval:
  dir: ${data.label}${radius.label}${select.label}${model.label}
  files:
    - "{stage:03d}stats.csv"
figs:
  dir: figs
trace:
  dir: trace
