dir: outputs
stats:
  dir: ${data.label}
  files:
    - stats.npz
    - simgs.npz
find:
  dir: ${data.label}${radius.label}
  files:
    - find.npz
make:
  dir: ${data.label}${radius.label}${density.label}
  files:
    - "{stage:03d}footprint.npy"
    - "{stage:03d}peaks.csv"
clean:
  dir: ${data.label}${radius.label}${density.label}${model.label}
  files:
    - "{stage:03d}footprint.npy"
    - "{stage:03d}peaks.csv"
spatial:
  dir: ${data.label}${radius.label}${density.label}${model.label}
  files:
    - "{stage:03d}segment.npy"
temporal:
  dir: ${data.label}${radius.label}${density.label}${model.label}
  files:
    - "{stage:03d}spike.npy"
    - "{stage:03d}background.npy"
figs:
  dir: figs
trace:
  dir: trace
