# @package _global_

defaults:
  - /data: Kd32
  - /radius: miniature
  - /init: min31

  - /cmd: Kd32
  - /dynamics: default
  - /penalty: annealing
  - /clean: default

  - /env: default
  - /outputs: default
  - _self_

clean:
  temporal:
    thr_bg_rsn: 10.0
early_stop: false
max_train_step: 30
