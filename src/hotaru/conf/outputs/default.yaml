dir: outputs
normalize:
  dir: ${data.label}
  files:
    - stats.npz
    - max.npy
    - std.npy
    - cor.npy
init:
  dir: ${data.label}/${init.label}
  files:
    - "{stage:03d}footprint.npy"
    - "{stage:03d}stats.csv"
spatial:
  dir: ${data.label}/${init.label}/${model.label}
  files:
    - "{stage:03d}footprints.npy"
    - "{stage:03d}stats.csv"
temporal:
  dir: ${data.label}/${init.label}/${model.label}
  files:
    - "{stage:03d}spike.npy"
    - "{stage:03d}background.npy"
eval:
  dir: ${data.label}/${init.label}/${model.label}
  files:
    - "{stage:03d}stats.csv"
figs:
  dir: figs
trace:
  dir: trace
