# @package _global_

defaults:
  - /data: CA3
  - /radius: miniature
  - /init: min27

  - /cmd: CA3
  - /dynamics: default
  - /penalty: annealing
  - /clean: default

  - /env: default
  - /outputs: default
  - _self_

clean:
  temporal:
    thr_bg_rsn: 3.0
early_stop: false
max_train_step: 30
